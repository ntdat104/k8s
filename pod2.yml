apiVersion: v1
kind: Pod
metadata:
  name: simple-app
  labels:
    app: simple-app
    env: dev
spec:
  containers:
    - name: simple-app
      image: vietaws/eks:v2
      ports:
        - containerPort: 8080
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
